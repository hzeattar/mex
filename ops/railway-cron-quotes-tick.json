{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "php api/cron/quotes_tick.php --token=$CRON_KEY",
    "restartPolicyType": "ALWAYS",
    "healthcheckTimeout": 30
  },
  "cron": {
    "schedule": "*/1 * * * *",
    "timezone": "UTC"
  }
}