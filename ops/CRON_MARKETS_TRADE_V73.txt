# ضع هذه السطور داخل cPanel > Cron Jobs
# ثم احذف السطور القديمة الخاصة بـ quotes_tick / market_ingest / markets_sync إن كانت موجودة بتوقيت أبطأ.

* * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/quotes_tick.php?token=$CRON_KEY" >/dev/null 2>&1
* * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/market_ingest.php?token=$CRON_KEY" >/dev/null 2>&1
*/5 * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/cache_reset.php?token=$CRON_KEY" >/dev/null 2>&1
*/15 * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/markets_sync.php?token=$CRON_KEY" >/dev/null 2>&1
*/5 * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/holds_expire.php?token=$CRON_KEY" >/dev/null 2>&1
0 * * * * curl -fsS "https://mex-production.up.railway.app/api/cron/invest_tick.php?token=$CRON_KEY" >/dev/null 2>&1
